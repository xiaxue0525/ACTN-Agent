# ACTAgent Nextcloud Talk

Official ACTAgent channel plugin for Nextcloud Talk conversations.

Install from ACTAgent:

```bash
actagent plugin add @actagent/nextcloud-talk
```

Configure the Nextcloud server and Talk credentials in ACTAgent, then enable the conversations where agents should receive and send messages.
