# ACTAgent Discord

Official ACTAgent channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from ACTAgent:

```bash
actagent plugin add @actagent/discord
```

Configure a Discord bot token and the channels or servers ACTAgent should handle. The plugin lets ACTAgent agents receive Discord messages and respond through the configured Discord app.
