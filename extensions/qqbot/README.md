# ACTAgent QQ Bot

Official ACTAgent channel plugin for QQ Bot group and direct-message workflows.

Install from ACTAgent:

```bash
actagent plugin add @actagent/qqbot
```

Configure QQ Bot credentials in ACTAgent, then connect the bot to the groups or direct-message contexts where agents should operate.
