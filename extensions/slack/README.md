# ACTAgent Slack

Official ACTAgent channel plugin for Slack channels, DMs, commands, and app events.

Install from ACTAgent:

```bash
actagent plugin add @actagent/slack
```

Configure the Slack app credentials and allowed workspaces/channels in ACTAgent. The plugin lets agents receive Slack events and reply through the configured Slack app.
