# ACTAgent LINE

Official ACTAgent channel plugin for LINE Bot API chats.

Install from ACTAgent:

```bash
actagent plugin add @actagent/line
```

Configure LINE channel credentials in ACTAgent, then connect the bot to the chats where agents should receive and send messages.
