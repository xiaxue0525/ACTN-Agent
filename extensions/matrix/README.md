# ACTAgent Matrix

Official ACTAgent channel plugin for Matrix rooms and direct messages.

Install from ACTAgent:

```bash
actagent plugin add @actagent/matrix
```

Configure the Matrix homeserver and bot credentials in ACTAgent. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.
