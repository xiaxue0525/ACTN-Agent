# ACTAgent Microsoft Teams

Official ACTAgent channel plugin for Microsoft Teams bot conversations.

Install from ACTAgent:

```bash
actagent plugin add @actagent/msteams
```

Configure the Teams bot credentials and trusted service URLs in ACTAgent, then connect the bot to the teams or chats where agents should operate.
