# ACTAgent Anthropic Vertex Provider

Official ACTAgent provider plugin for Claude models hosted through Google Vertex AI.

Install from ACTAgent:

```bash
actagent plugin add @actagent/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in ACTAgent, then select Claude models with the Anthropic Vertex provider.
