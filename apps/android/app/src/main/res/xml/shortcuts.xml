<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_ACTAGENT"
        app:queryPatterns="@array/ask_actagent_query_patterns">
        <intent
            android:action="ai.actagent.app.action.ASK_ACTAGENT"
            android:targetPackage="ai.actagent.app"
            android:targetClass="ai.actagent.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
