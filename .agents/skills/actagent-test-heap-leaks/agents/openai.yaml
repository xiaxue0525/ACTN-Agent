interface:
  display_name: "Test Heap Leaks"
  short_description: "Investigate test OOMs with heap snapshots"
  default_prompt: "Use $actagent-test-heap-leaks to investigate test memory growth with heap snapshots and reduce its impact."
