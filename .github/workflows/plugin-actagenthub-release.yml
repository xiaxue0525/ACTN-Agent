name: Plugin ACTAgentHub Release

on:
  workflow_dispatch:
    inputs:
      publish_scope:
        description: Publish the selected plugins or all ACTAgentHub-publishable plugins from the workflow ref
        required: true
        default: selected
        type: choice
        options:
          - selected
          - all-publishable
      plugins:
        description: Comma-separated plugin package names to publish when publish_scope=selected
        required: false
        type: string
      ref:
        description: Commit SHA on main, a release branch, or the matching tideactagent alpha branch to publish from; defaults to the workflow ref
        required: false
        default: ""
        type: string
      release_publish_run_id:
        description: Approved ACTAgent Release Publish workflow run id
        required: false
        type: string

concurrency:
  group: plugin-actagenthub-release-${{ github.event_name == 'workflow_dispatch' && inputs.ref || github.sha }}
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  NODE_VERSION: "24.15.0"
  ACTAGENTHUB_REGISTRY: "https://actagenthub.ai"
  ACTAGENTHUB_REPOSITORY: "actagent/actagenthub"
  # Pinned to a reviewed ACTAgentHub commit so release behavior stays reproducible.
  ACTAGENTHUB_REF: "facf20ceb6cc459e2872d941e71335a784bbc55c"

jobs:
  preview_plugins_actagenthub:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      ref_revision: ${{ steps.ref.outputs.sha }}
      has_candidates: ${{ steps.plan.outputs.has_candidates }}
      candidate_count: ${{ steps.plan.outputs.candidate_count }}
      skipped_published_count: ${{ steps.plan.outputs.skipped_published_count }}
      matrix: ${{ steps.plan.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
        with:
          persist-credentials: false
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Setup Node environment
        uses: ./.github/actions/setup-node-env
        with:
          node-version: ${{ env.NODE_VERSION }}
          install-bun: "false"

      - name: Resolve checked-out ref
        id: ref
        env:
          TARGET_REF: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || '' }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin \
            +refs/heads/main:refs/remotes/origin/main \
            '+refs/heads/release/*:refs/remotes/origin/release/*'
          if [[ -n "${TARGET_REF}" ]]; then
            if git rev-parse --verify --quiet "${TARGET_REF}^{commit}" >/dev/null; then
              target_sha="$(git rev-parse "${TARGET_REF}^{commit}")"
            elif git rev-parse --verify --quiet "origin/${TARGET_REF}^{commit}" >/dev/null; then
              target_sha="$(git rev-parse "origin/${TARGET_REF}^{commit}")"
            else
              echo "Unable to resolve requested publish ref: ${TARGET_REF}" >&2
              exit 1
            fi
            git checkout --detach "${target_sha}"
          fi
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Validate ref is on a trusted publish branch
        env:
          WORKFLOW_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          if git merge-base --is-ancestor HEAD origin/main; then
            exit 0
          fi
          while IFS= read -r release_ref; do
            if git merge-base --is-ancestor HEAD "${release_ref}"; then
              exit 0
            fi
          done < <(git for-each-ref --format='%(refname)' refs/remotes/origin/release)
          if [[ "${WORKFLOW_REF}" =~ ^refs/heads/tideactagent/alpha/[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{4}Z$ ]]; then
            alpha_branch="${WORKFLOW_REF#refs/heads/}"
            git fetch --no-tags origin "+refs/heads/${alpha_branch}:refs/remotes/origin/${alpha_branch}"
            if git merge-base --is-ancestor HEAD "refs/remotes/origin/${alpha_branch}"; then
              exit 0
            fi
          fi
          echo "Plugin ACTAgentHub publishes must target a commit reachable from main, release/*, or the matching tideactagent alpha branch." >&2
          exit 1

      - name: Validate publishable plugin metadata
        env:
          PUBLISH_SCOPE: ${{ github.event_name == 'workflow_dispatch' && inputs.publish_scope || '' }}
          RELEASE_PLUGINS: ${{ github.event_name == 'workflow_dispatch' && inputs.plugins || '' }}
          BASE_REF: ${{ github.event_name != 'workflow_dispatch' && github.event.before || '' }}
          HEAD_REF: ${{ steps.ref.outputs.sha }}
        run: |
          set -euo pipefail
          if [[ -n "${PUBLISH_SCOPE}" ]]; then
            release_args=(--selection-mode "${PUBLISH_SCOPE}")
            if [[ -n "${RELEASE_PLUGINS}" ]]; then
              release_args+=(--plugins "${RELEASE_PLUGINS}")
            fi
            pnpm release:plugins:actagenthub:check -- "${release_args[@]}"
          elif [[ -n "${BASE_REF}" ]]; then
            pnpm release:plugins:actagenthub:check -- --base-ref "${BASE_REF}" --head-ref "${HEAD_REF}"
          else
            pnpm release:plugins:actagenthub:check
          fi

      - name: Resolve plugin release plan
        id: plan
        env:
          PUBLISH_SCOPE: ${{ github.event_name == 'workflow_dispatch' && inputs.publish_scope || '' }}
          RELEASE_PLUGINS: ${{ github.event_name == 'workflow_dispatch' && inputs.plugins || '' }}
          BASE_REF: ${{ github.event_name != 'workflow_dispatch' && github.event.before || '' }}
          HEAD_REF: ${{ steps.ref.outputs.sha }}
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
        run: |
          set -euo pipefail
          mkdir -p .local
          if [[ -n "${PUBLISH_SCOPE}" ]]; then
            plan_args=(--selection-mode "${PUBLISH_SCOPE}")
            if [[ -n "${RELEASE_PLUGINS}" ]]; then
              plan_args+=(--plugins "${RELEASE_PLUGINS}")
            fi
            node --import tsx scripts/plugin-actagenthub-release-plan.ts "${plan_args[@]}" > .local/plugin-actagenthub-release-plan.json
          elif [[ -n "${BASE_REF}" ]]; then
            node --import tsx scripts/plugin-actagenthub-release-plan.ts --base-ref "${BASE_REF}" --head-ref "${HEAD_REF}" > .local/plugin-actagenthub-release-plan.json
          else
            node --import tsx scripts/plugin-actagenthub-release-plan.ts > .local/plugin-actagenthub-release-plan.json
          fi

          cat .local/plugin-actagenthub-release-plan.json

          candidate_count="$(jq -r '.candidates | length' .local/plugin-actagenthub-release-plan.json)"
          skipped_published_count="$(jq -r '.skippedPublished | length' .local/plugin-actagenthub-release-plan.json)"
          has_candidates="false"
          if [[ "${candidate_count}" != "0" ]]; then
            has_candidates="true"
          fi
          matrix_json="$(jq -c '.candidates' .local/plugin-actagenthub-release-plan.json)"

          {
            echo "candidate_count=${candidate_count}"
            echo "skipped_published_count=${skipped_published_count}"
            echo "has_candidates=${has_candidates}"
            echo "matrix=${matrix_json}"
          } >> "$GITHUB_OUTPUT"

          echo "Plugin release candidates:"
          jq -r '.candidates[]? | "- \(.packageName)@\(.version) [\(.publishTag)] from \(.packageDir)"' .local/plugin-actagenthub-release-plan.json

          echo "Already published / skipped:"
          jq -r '.skippedPublished[]? | "- \(.packageName)@\(.version)"' .local/plugin-actagenthub-release-plan.json

      - name: Fail manual publish when target versions already exist
        if: github.event_name == 'workflow_dispatch' && inputs.publish_scope == 'selected' && steps.plan.outputs.skipped_published_count != '0'
        run: |
          echo "::error::One or more selected plugin versions already exist on ACTAgentHub. Bump the version before running a real publish."
          exit 1

      - name: Validate tideactagent alpha plugin channels
        if: startsWith(github.ref, 'refs/heads/tideactagent/alpha/')
        run: |
          set -euo pipefail
          invalid="$(
            jq -r '.candidates[]? | select(.publishTag != "alpha" or .channel != "alpha") | "- \(.packageName)@\(.version) [\(.publishTag)]"' .local/plugin-actagenthub-release-plan.json
          )"
          if [[ -n "${invalid}" ]]; then
            echo "tideactagent alpha ACTAgentHub publishes may only publish alpha plugin versions." >&2
            printf '%s\n' "${invalid}" >&2
            exit 1
          fi

      - name: Verify ACTAgent ACTAgentHub package ownership
        if: steps.plan.outputs.has_candidates == 'true'
        env:
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
        run: node --import tsx scripts/plugin-actagenthub-owner-preflight.ts .local/plugin-actagenthub-release-plan.json

  validate_release_publish_approval:
    name: Validate release publish approval
    needs: preview_plugins_actagenthub
    if: github.event_name == 'workflow_dispatch' && needs.preview_plugins_actagenthub.outputs.has_candidates == 'true'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
        with:
          persist-credentials: false

      - name: Validate release publish approval run
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_PUBLISH_RUN_ID: ${{ inputs.release_publish_run_id }}
          EXPECTED_WORKFLOW_BRANCH: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if [[ -z "${RELEASE_PUBLISH_RUN_ID// }" ]]; then
            if [[ "${GITHUB_ACTOR}" == "github-actions[bot]" ]]; then
              echo "Plugin ACTAgentHub publish dispatched by another workflow must include release_publish_run_id." >&2
              exit 1
            fi
            echo "Direct Plugin ACTAgentHub Release dispatch; relying on this workflow's actagenthub-plugin-release environment approval."
            exit 0
          fi
          direct_recovery=false
          if [[ "${GITHUB_ACTOR}" != "github-actions[bot]" ]]; then
            direct_recovery=true
            echo "Direct Plugin ACTAgentHub Release recovery with release_publish_run_id; relying on this workflow's actagenthub-plugin-release environment approval."
          fi
          RUN_JSON="$(gh run view "$RELEASE_PUBLISH_RUN_ID" --repo "$GITHUB_REPOSITORY" --json workflowName,headBranch,event,status,conclusion,url)"
          printf '%s' "$RUN_JSON" | DIRECT_RELEASE_RECOVERY="${direct_recovery}" node scripts/validate-release-publish-approval.mjs

  preview_plugin_pack:
    needs: preview_plugins_actagenthub
    if: needs.preview_plugins_actagenthub.outputs.has_candidates == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        plugin: ${{ fromJson(needs.preview_plugins_actagenthub.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
        with:
          persist-credentials: false
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Checkout target revision
        env:
          TARGET_SHA: ${{ needs.preview_plugins_actagenthub.outputs.ref_revision }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin \
            +refs/heads/main:refs/remotes/origin/main \
            '+refs/heads/release/*:refs/remotes/origin/release/*'
          git checkout --detach "${TARGET_SHA}"

      - name: Setup Node environment
        uses: ./.github/actions/setup-node-env
        with:
          node-version: ${{ env.NODE_VERSION }}
          install-bun: "true"
          install-deps: "true"

      - name: Checkout ACTAgentHub CLI source
        uses: actions/checkout@v6.0.3
        with:
          persist-credentials: false
          repository: ${{ env.ACTAGENTHUB_REPOSITORY }}
          ref: main
          path: actagenthub-source
          fetch-depth: 0

      - name: Checkout pinned ACTAgentHub CLI revision
        working-directory: actagenthub-source
        env:
          ACTAGENTHUB_REF: ${{ env.ACTAGENTHUB_REF }}
        run: git checkout --detach "${ACTAGENTHUB_REF}"

      - name: Install ACTAgentHub CLI dependencies
        working-directory: actagenthub-source
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            if bun install --frozen-lockfile; then
              exit 0
            fi
            status="$?"
            if [[ "${attempt}" == "3" ]]; then
              exit "${status}"
            fi
            echo "bun install failed while preparing ACTAgentHub CLI; retrying (${attempt}/3)."
            rm -rf node_modules "${RUNNER_TEMP}/bun-install-cache" || true
            sleep $((attempt * 15))
          done

      - name: Bootstrap ACTAgentHub CLI
        run: |
          cat > "$RUNNER_TEMP/actagenthub" <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail
          exec bun "$GITHUB_WORKSPACE/actagenthub-source/packages/actagenthub/src/cli.ts" "$@"
          EOF
          chmod +x "$RUNNER_TEMP/actagenthub"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"

      - name: Verify package-local runtime build
        run: node scripts/check-plugin-npm-runtime-builds.mjs --package "${{ matrix.plugin.packageDir }}"

      - name: Preview publish command
        env:
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
          SOURCE_REPO: ${{ github.repository }}
          SOURCE_COMMIT: ${{ needs.preview_plugins_actagenthub.outputs.ref_revision }}
          SOURCE_REF: ${{ github.ref }}
          PACKAGE_TAG: ${{ matrix.plugin.publishTag }}
          PACKAGE_DIR: ${{ matrix.plugin.packageDir }}
        run: bash scripts/plugin-actagenthub-publish.sh --dry-run "${PACKAGE_DIR}"

  publish_plugins_actagenthub:
    needs: [preview_plugins_actagenthub, preview_plugin_pack, validate_release_publish_approval]
    if: github.event_name == 'workflow_dispatch' && needs.preview_plugins_actagenthub.outputs.has_candidates == 'true'
    runs-on: ubuntu-latest
    environment: actagenthub-plugin-release
    permissions:
      actions: read
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      max-parallel: 32
      matrix:
        plugin: ${{ fromJson(needs.preview_plugins_actagenthub.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
        with:
          persist-credentials: false
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Checkout target revision
        env:
          TARGET_SHA: ${{ needs.preview_plugins_actagenthub.outputs.ref_revision }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin \
            +refs/heads/main:refs/remotes/origin/main \
            '+refs/heads/release/*:refs/remotes/origin/release/*'
          git checkout --detach "${TARGET_SHA}"

      - name: Setup Node environment
        uses: ./.github/actions/setup-node-env
        with:
          node-version: ${{ env.NODE_VERSION }}
          install-bun: "true"
          install-deps: "true"

      - name: Checkout ACTAgentHub CLI source
        uses: actions/checkout@v6.0.3
        with:
          persist-credentials: false
          repository: ${{ env.ACTAGENTHUB_REPOSITORY }}
          ref: main
          path: actagenthub-source
          fetch-depth: 0

      - name: Checkout pinned ACTAgentHub CLI revision
        working-directory: actagenthub-source
        env:
          ACTAGENTHUB_REF: ${{ env.ACTAGENTHUB_REF }}
        run: git checkout --detach "${ACTAGENTHUB_REF}"

      - name: Install ACTAgentHub CLI dependencies
        working-directory: actagenthub-source
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            if bun install --frozen-lockfile; then
              exit 0
            fi
            status="$?"
            if [[ "${attempt}" == "3" ]]; then
              exit "${status}"
            fi
            echo "bun install failed while preparing ACTAgentHub CLI; retrying (${attempt}/3)."
            rm -rf node_modules "${RUNNER_TEMP}/bun-install-cache" || true
            sleep $((attempt * 15))
          done

      - name: Bootstrap ACTAgentHub CLI
        run: |
          cat > "$RUNNER_TEMP/actagenthub" <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail
          exec bun "$GITHUB_WORKSPACE/actagenthub-source/packages/actagenthub/src/cli.ts" "$@"
          EOF
          chmod +x "$RUNNER_TEMP/actagenthub"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"

      - name: Write ACTAgentHub token config
        env:
          ACTAGENTHUB_TOKEN: ${{ secrets.ACTAGENTHUB_TOKEN }}
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
        run: |
          set -euo pipefail
          if [[ -z "${ACTAGENTHUB_TOKEN}" ]]; then
            echo "No ACTAGENTHUB_TOKEN secret configured; publish will rely on GitHub OIDC trusted publishing."
            exit 0
          fi
          node --input-type=module <<'EOF'
          import { writeFileSync } from "node:fs";
          import { join } from "node:path";

          const path = join(process.env.RUNNER_TEMP, "actagenthub-config.json");
          writeFileSync(
            path,
            `${JSON.stringify(
              {
                registry: process.env.ACTAGENTHUB_REGISTRY,
                token: process.env.ACTAGENTHUB_TOKEN,
              },
              null,
              2,
            )}\n`,
          );
          console.log(path);
          EOF
          echo "ACTAGENTHUB_CONFIG_PATH=${RUNNER_TEMP}/actagenthub-config.json" >> "$GITHUB_ENV"

      - name: Check ACTAgentHub package version
        id: actagenthub_package_version
        env:
          PACKAGE_NAME: ${{ matrix.plugin.packageName }}
          PACKAGE_VERSION: ${{ matrix.plugin.version }}
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
        run: |
          set -euo pipefail
          encoded_name="$(node -e 'console.log(encodeURIComponent(process.env.PACKAGE_NAME ?? ""))')"
          encoded_version="$(node -e 'console.log(encodeURIComponent(process.env.PACKAGE_VERSION ?? ""))')"
          url="${ACTAGENTHUB_REGISTRY%/}/api/v1/packages/${encoded_name}/versions/${encoded_version}"
          status=""
          for attempt in $(seq 1 8); do
            status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' "${url}")"
            if [[ "${status}" == "404" || "${status}" =~ ^2 ]]; then
              break
            fi
            if [[ "${status}" == "429" || "${status}" =~ ^5 ]]; then
              echo "ACTAgentHub availability check returned ${status} for ${PACKAGE_NAME}@${PACKAGE_VERSION}; retrying (${attempt}/8)."
              sleep 60
              continue
            fi
            break
          done
          if [[ "${status}" =~ ^2 ]]; then
            echo "${PACKAGE_NAME}@${PACKAGE_VERSION} is already published on ACTAgentHub."
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [[ "${status}" != "404" ]]; then
            echo "Unexpected ACTAgentHub response (${status}) for ${PACKAGE_NAME}@${PACKAGE_VERSION}."
            exit 1
          fi
          echo "already_published=false" >> "$GITHUB_OUTPUT"

      - name: Publish
        if: steps.actagenthub_package_version.outputs.already_published != 'true'
        env:
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
          SOURCE_REPO: ${{ github.repository }}
          SOURCE_COMMIT: ${{ needs.preview_plugins_actagenthub.outputs.ref_revision }}
          SOURCE_REF: ${{ github.ref }}
          PACKAGE_TAG: ${{ matrix.plugin.publishTag }}
          PACKAGE_DIR: ${{ matrix.plugin.packageDir }}
        run: bash scripts/plugin-actagenthub-publish.sh --publish "${PACKAGE_DIR}"

      - name: Verify published ACTAgentHub package
        env:
          ACTAGENTHUB_REGISTRY: ${{ env.ACTAGENTHUB_REGISTRY }}
          PACKAGE_NAME: ${{ matrix.plugin.packageName }}
          PACKAGE_VERSION: ${{ matrix.plugin.version }}
          PACKAGE_TAG: ${{ matrix.plugin.publishTag }}
        run: |
          set -euo pipefail
          node --input-type=module <<'EOF'
          const registry = (process.env.ACTAGENTHUB_REGISTRY ?? "https://actagenthub.ai").replace(/\/+$/, "");
          const packageName = process.env.PACKAGE_NAME;
          const packageVersion = process.env.PACKAGE_VERSION;
          const packageTag = process.env.PACKAGE_TAG;
          if (!packageName || !packageVersion || !packageTag) {
            throw new Error("Missing ACTAgentHub package verification env.");
          }
          const encodedName = encodeURIComponent(packageName);
          const encodedVersion = encodeURIComponent(packageVersion);
          const detailUrl = `${registry}/api/v1/packages/${encodedName}`;
          const versionUrl = `${detailUrl}/versions/${encodedVersion}`;
          const artifactUrl = `${versionUrl}/artifact/download`;

          async function fetchWithRetry(url, options = {}) {
            let lastStatus = "unknown";
            for (let attempt = 1; attempt <= 12; attempt += 1) {
              try {
                const response = await fetch(url, { redirect: "manual", ...options });
                lastStatus = response.status;
                if (response.status !== 429 && response.status < 500) {
                  return response;
                }
              } catch (error) {
                lastStatus = error instanceof Error ? error.message : String(error);
              }
              await new Promise((resolve) => setTimeout(resolve, attempt * 5000));
            }
            throw new Error(`${url} did not stabilize; last status ${lastStatus}.`);
          }

          const detailResponse = await fetchWithRetry(detailUrl, {
            headers: { accept: "application/json" },
          });
          if (!detailResponse.ok) {
            throw new Error(`${detailUrl} returned HTTP ${detailResponse.status}.`);
          }
          const detail = await detailResponse.json();
          const tags = detail?.package?.tags ?? {};
          if (tags[packageTag] !== packageVersion) {
            throw new Error(
              `${packageName}: ACTAgentHub tag ${packageTag} points to ${tags[packageTag] ?? "<missing>"}, expected ${packageVersion}.`,
            );
          }
          const versionResponse = await fetchWithRetry(versionUrl);
          if (!versionResponse.ok) {
            throw new Error(`${versionUrl} returned HTTP ${versionResponse.status}.`);
          }
          const artifactResponse = await fetchWithRetry(artifactUrl, { method: "HEAD" });
          if (artifactResponse.status < 200 || artifactResponse.status >= 400) {
            throw new Error(`${artifactUrl} returned HTTP ${artifactResponse.status}.`);
          }
          console.log(`${packageName}@${packageVersion} verified on ACTAgentHub.`);
          EOF
